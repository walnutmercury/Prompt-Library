You are a MicroStrategy to Power BI migration assistant.
You generate complete Power BI Project (PBIP) folders
from MicroStrategy Report Details and SQL.

A PBIP folder contains two layers.

SEMANTIC MODEL LAYER
Location: ReportName.SemanticModel/definition/

Create a tables folder with one file per query table.
Each table file is TMDL format.
Use this exact DB2 connection pattern for each table:

partition
    type: m
    mode: import
    source =
        let
            Source = DB2.Database(
                "SERVERNAME:PORT",
                "DATABASE",
                [
                    HierarchicalNavigation=true,
                    Implementation="Microsoft",
                    Query="SQL GOES HERE"
                ]
            )
        in
            Source

Replace SERVERNAME:PORT and DATABASE with placeholders
clearly marked for the user to fill in.
Embed the cleaned SQL in the Query parameter.

Create DAX measures for any metrics that are not
already resolved in the SQL.
Flag metrics that need DAX as REQUIRES DAX and write
a placeholder measure with a comment explaining
what logic is needed.

REPORT LAYER
Location: ReportName.Report/definition/pages/

Create one page folder called Page1.
Inside Page1 create a visuals folder.
Create one table visual JSON file showing all columns
in Template order using MicroStrategy display names.
Create one slicer visual JSON file per prompt parameter.
Create a title text box JSON using the report description.

PARSING RULES
Report Description: use as title text box content
Prompts: each becomes a slicer visual and a parameter
Report Filter: extract hidden prompts, add as slicers
Template: use column order and display names exactly
Remarks: ignore
Metrics: SQL-resolved needs no DAX, flag others
SQL: clean DB2 syntax as follows
  Remove WITH UR
  Replace FETCH FIRST n ROWS ONLY with LIMIT n
  Replace DAYS(x)-DAYS(y) with DATEDIFF(day,x,y)
  Replace CHAR(col) with CAST(col AS VARCHAR)
  Replace DECIMAL(p,s) with NUMBER(p,s)

OUTPUT ORDER
1. CLASSIFICATION: pattern, tier, prompts found,
   metrics status, any blockers
2. Write all TMDL and JSON files to the PBIP folder
   structure in the current working directory
3. Confirm all files written with paths listed



Report name: [name here]
PBIP folder path: [path to the .pbip folder you created]

Report Details:
[paste everything here]

SQL Statement:
[paste SQL here]

Generate the complete PBIP folder structure and
write all files to disk.



---Errors---

Please fix all issues in the PBIP folder and rewrite 
all affected files to disk.

CONNECTION
Update the DB2 connection in the TMDL file to use 
these exact details:
Server and port: [your server:port here]
Database: [your database here]
Username: [your username here]

Use this exact connection pattern, no variations:
Source = DB2.Database(
    "SERVERNAME:PORT",
    "DATABASE",
    [
        HierarchicalNavigation=true,
        Implementation="Microsoft",
        Query="SQL HERE"
    ]
)

Do not embed the password in any file.
Credentials will be entered in Power BI Desktop.

SQL
Embed the original report SQL in the Query parameter.
Add FETCH FIRST 100 ROWS ONLY at the end of the SQL
for testing purposes.

LINE FEEDS
Find and remove all instances of #(lf) in every 
file in the PBIP folder.
Replace with a single space or remove entirely 
depending on context.

REPORT VISUALS
Ensure PBIR visual JSON files exist at this path:
[your report name].Report/definition/pages/Page1/visuals/

Regenerate all visuals including:
One table visual with all columns in Template order
using MicroStrategy display names as column headers.
One slicer visual per prompt with the prompt name
as the slicer title.
One title text box using the report description.

After completing all fixes list every file written 
with its full path so I can confirm before reopening 
in Power BI Desktop.


---Visuals didn't load -----

The semantic model loaded successfully and data 
refreshed correctly. However no visuals appeared 
on the report canvas.

Please check the PBIR visual JSON files at:
[report name].Report/definition/pages/Page1/visuals/

List the files that exist there. Then check each 
visual JSON file and confirm:
- The $schema field is present and correct
- The column bindings reference columns that exist 
  in the semantic model by their exact names
- Run this command to list the actual column names 
  that loaded into the semantic model:
  pbi --json column list

Regenerate all visual JSON files using the exact 
column names from the semantic model and rewrite 
to disk. Then close Power BI Desktop without saving 
and reopen to load the corrected visuals.

----------------------------   Rename

The semantic model loaded successfully but column 
names in the TMDL file use SQL aliases which are 
different from the MicroStrategy display names 
in the Template section.

Please do the following.

STEP 1 - Read the Template section from the 
Report Details I provided earlier and extract 
the exact display name for each column in order.

STEP 2 - Read the SQL and identify the column 
alias for each SELECT column in the same order.

STEP 3 - In the TMDL file add a rename step 
after the Source step that maps each SQL alias 
to its MicroStrategy display name like this:

RenamedColumns = Table.RenameColumns(
    Source,
    {
        {"sql_alias_1", "Display Name 1"},
        {"sql_alias_2", "Display Name 2"}
    }
),

STEP 4 - Regenerate the PBIR table visual JSON 
using the MicroStrategy display names as the 
column bindings not the SQL aliases.

STEP 5 - Rewrite all affected files to disk 
and list every file changed.







The semantic model is working and column names are 
correct. Now generate the PBIR visual JSON files 
and write them to disk.

First run this command to get the exact column 
names from the loaded semantic model:
pbi --json column list

Then use those exact column names to generate:

VISUAL 1 - Table
File: [report name].Report/definition/pages/Page1/visuals/table1/visual.json
A table visual showing all columns in this order:
[paste the Template column order from your Report Details here]

VISUAL 2 onwards - One slicer per prompt
File: [report name].Report/definition/pages/Page1/visuals/slicer1/visual.json
One slicer visual per prompt from the Report Details.
Use the prompt display name as the slicer title.
Bind each slicer to its corresponding column.

TITLE - Text box
File: [report name].Report/definition/pages/Page1/visuals/title1/visual.json
A text box visual at the top of the canvas showing 
the report description as the title.

Use PBIR JSON schema version that matches what 
Power BI Desktop created when the PBIP was saved.
Check the existing page JSON file to confirm 
the correct schema version before generating.

After writing all files list every file path 
confirmed written to disk.

Important: do not close or touch Power BI Desktop.
I will close and reopen it manually after you confirm.



---Failing Schema---The PBIR visual JSON files are failing with this error:
property visualContainers has not been defined and 
the schema does not allow additional properties.

This means the generated JSON is using the old 
PBIR-legacy format not the current PBIR format.

Please do all of the following in one pass.

STEP 1 - Check the page schema
Read the page JSON file at this exact path:
[report name].Report/definition/pages/Page1/page.json
Show me its contents to confirm the correct schema 
version Power BI Desktop is expecting.

STEP 2 - Check for existing valid visuals
Check if any visual folders exist at:
[report name].Report/definition/pages/Page1/visuals/
If any valid visual JSON files exist read one and 
use its exact structure as the template for all 
new visuals. Do not invent the schema from memory.

STEP 3 - Get column names from the TMDL file
Read the TMDL table file and extract the column 
names exactly as they appear after the rename step.
These are the names to use for all visual bindings.
List them so I can confirm they are correct before 
generating visuals.

STEP 4 - Generate all visuals
Using the schema from step 2 and column names 
from step 3 generate these visuals:

One table visual showing all columns in Template 
order at:
[report name].Report/definition/pages/Page1/visuals/table1/visual.json

One slicer visual per prompt from the Report Details 
using the prompt display name as the slicer title 
bound to its corresponding column at:
[report name].Report/definition/pages/Page1/visuals/slicer1/visual.json
[report name].Report/definition/pages/Page1/visuals/slicer2/visual.json
and so on for each prompt.

One title text box showing the report description at:
[report name].Report/definition/pages/Page1/visuals/title1/visual.json

STEP 5 - Write and confirm
Write all files to disk and list every file path 
confirmed written so I can close and reopen 
Power BI Desktop to validate.

STEP 5 - Write and confirm
Write all files to disk and list every file path 
confirmed written so I can close and reopen 
Power BI Desktop to validate.

--- Found it needs GUID ---
Look inside this folder:
[report name].Report/definition/pages/

List everything you find there including all 
folder names and file names.
Do not assume the folder is called Page1.
Read whatever folder name actually exists there
and use that exact name for all subsequent steps.


Do not assume any folder names. Discover everything 
automatically by reading the actual file system.

STEP 1 - Discover the page folder
Look inside:
[report name].Report/definition/pages/

Find the folder that exists there. It will have 
a GUID name. Store that GUID and use it for all 
subsequent steps instead of Page1 or any hardcoded name.

STEP 2 - Read column names
Find and read the TMDL table file inside:
[report name].SemanticModel/definition/tables/

List all column names exactly as they appear 
after the rename step.

STEP 3 - Fetch the PBIR visual schema
Fetch this URL and read it:
https://developer.microsoft.com/json-schemas/fabric/item/report/definition/visualContainer/2.1.0/schema.json

Use only properties defined in that schema.
Do not use any properties not in the schema.
Do not use visualContainers anywhere.

STEP 4 - Create visuals folder
Create a visuals folder inside the discovered 
page GUID folder.

STEP 5 - Generate table visual only
Create a table visual JSON file inside the 
visuals folder using the schema from step 3 
and columns from step 2 in Template order.

STEP 6 - Confirm and stop
List every file written with full path.
Stop here and wait before doing anything else.
